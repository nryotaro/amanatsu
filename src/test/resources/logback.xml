<configuration>

  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <!-- encoders are assigned the type
         ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <appender name="VERBOSE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%msg%n</pattern>
    </encoder>
  </appender>

  <logger name="org.springframework" level="ERROR"/>

  <logger name="verbose" level="INFO" additivity="false">
    <appender-ref ref="VERBOSE"/>
  </logger>

  <logger name="org.nryotaro.edgar.repository.IndexRepository" level="INFO"></logger>

  <root level="ERROR">
    <appender-ref ref="STDOUT" />
  </root>
</configuration>
